package lecture4

fun main() {

    // Kotlin은 객체간에 비교 연산자를 사용할 경우, 자동으로 compareTo를 호출

    // 동등성 : 객체의 값을 비교. (java) equals(), (Kotlin) == 연산자
    // 동일성 : 객체의 주소를 비교. (java) == 연산자, (Kotlin) === 연산자

}